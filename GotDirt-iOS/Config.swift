// Config.swift — Got Dirt? iOS
// TODO: Replace baseURL with your Vercel deployment URL before running.

import Foundation

enum Config {
    /// Your Vercel deployment URL — no trailing slash.
    static let baseURL = "https://gotdirt.us"
}
